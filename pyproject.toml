[project]
name = "localsr"
version = "0.1.0-beta"
description = "A local-first desktop application for image and video restoration using Tauri, PyTorch, and Spandrel."
authors = [{name = "Hermes Reisner"}]
readme = "README.md"
license = "MIT"
license-files = ["LICENSE", "THIRD_PARTY_NOTICES.md"]
keywords = ["super-resolution", "upscaling", "Tauri", "PyTorch", "Spandrel"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: MacOS X",
    "Environment :: Win32 (MS Windows)",
    "Environment :: X11 Applications",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
]
dependencies = [
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "spandrel>=0.4.0",
    "Pillow>=10.0.0",
    "rawpy>=0.25.1",
    # Current macOS Intel PyTorch wheels use the NumPy 1.x C API. NumPy 2.x
    # imports but leaves torch.from_numpy()/Tensor.numpy() unavailable.
    "numpy>=1.24.0,<2.0",
    "tifffile>=2023.0.0",
    "av>=12.0.0"
]
requires-python = ">=3.11"

[project.urls]
Repository = "https://github.com/HerRei/local-upscale"
Issues = "https://github.com/HerRei/local-upscale/issues"

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-timeout>=2.4.0",
    "ruff>=0.1.0",
    "pyright==1.1.411",
    "vulture==2.14",
]
video = [
    "omegaconf>=2.3.0",
    # Modern platforms use the release that fixes the custom-pipeline RCEs.
    "diffusers>=0.38.0,<0.39; platform_system != 'Darwin' or platform_machine != 'x86_64'",
    # Intel macOS is limited to a PyTorch build without torch.xpu, which
    # diffusers 0.36+ imports unconditionally. This legacy exception remains
    # alpha-only and is recorded as an unresolved beta security gate.
    "diffusers>=0.33.1,<0.36; platform_system == 'Darwin' and platform_machine == 'x86_64'",
    "rotary-embedding-torch>=0.5.3",
    "einops",
    "psutil",
    "tqdm",
    "gguf",
]
package = ["pyinstaller>=6.21.0,<7", "pillow>=10.0.0"]
face = [
    # 4.10 is the newest line with both macOS 12 x86_64 and arm64 wheels
    # while remaining compatible with the NumPy 1.x runtime required by the
    # one-release PyTorch 2.2.2 cross-build exception.
    "opencv-python-headless==4.10.0.84",
]

[build-system]
requires = ["setuptools>=83.0.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
localsr = "localsr.__main__:main"

[tool.ruff]
line-length = 100
target-version = "py311"
# Vendored third-party code is kept byte-faithful to upstream (Apache-2.0
# modification marking); it is not linted or reformatted.
extend-exclude = ["src/localsr/video_models/seedvr2/vendor"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
timeout = 300
timeout_method = "thread"
# Native codec deadlocks can hold the GIL and block Python's timeout thread.
# The C-level watchdog still emits thread stacks and fails the process.
faulthandler_timeout = 300
faulthandler_exit_on_timeout = true

# Start with the language-neutral protocol and pure pipeline configuration.
# Expand this boundary as additional engine modules receive complete types.
[tool.pyright]
include = ["src/localsr/protocol", "src/localsr/core/pipeline.py"]
extraPaths = ["src"]
pythonVersion = "3.11"
typeCheckingMode = "standard"
reportUnnecessaryTypeIgnoreComment = "error"

[tool.setuptools]
include-package-data = false

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"localsr.core" = ["benchmark_references.json"]
"localsr.video_models.seedvr2" = [
    "*.safetensors",
    "*.md",
    "configs_3b/*.yaml",
    "configs_7b/*.yaml",
    "src/models/video_vae_v3/*.yaml",
    "vendor/LICENSE",
    "vendor/models/video_vae_v3/*.yaml",
]
