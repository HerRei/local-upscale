# One-release Intel -> Apple Silicon cross-build environment for v0.0.11-alpha.
#
# PyTorch 2.2.2 / torchvision 0.17.2 are the final releases that publish
# matching CPython 3.11 Intel and Apple Silicon wheels. The Mac mini merges
# those wheels into a universal2 PyInstaller analysis environment, then emits
# an ARM64-only worker.  This known security debt is forbidden for beta and
# must not be reused by later release tags. Dependabot intentionally ignores
# this file because torch and torchvision are a proven inseparable wheel pair.
torch==2.2.2
torchvision==0.17.2
numpy==1.26.4
spandrel==0.4.2
Pillow==12.3.0
rawpy==0.25.1
tifffile==2026.3.3
av==14.2.0
slint==1.9.2a1
# YuNet face detection. 4.10 is the final OpenCV wheel line that preserves
# macOS 12 support and NumPy 1.x compatibility for this one-release runtime.
opencv-python-headless==4.10.0.84

# Video remains Labs / Experimental, but the hidden worker still carries the
# dependencies needed by the existing frame and SeedVR2 code paths.
omegaconf==2.3.1
# OmegaConf 2.3.x requires this exact pure-Python line. Upstream publishes it
# only as an sdist, so CI builds one pinned py3-none-any bootstrap wheel before
# resolving the two otherwise wheel-only macOS architecture sets.
antlr4-python3-runtime==4.9.3
diffusers==0.35.2
# 0.8.9 is the final release compatible with torch 2.2; 0.9+ requires 2.4.
rotary-embedding-torch==0.8.9
einops==0.8.2
psutil==7.2.2
tqdm==4.70.0
gguf==0.19.0
safetensors==0.8.0
